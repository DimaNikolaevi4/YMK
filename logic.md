# Логика генерации календарно-тематического плана (КТП) из рабочей программы (РП)

## Полное руководство для ИИ-ассистента

Данный файл содержит **исчерпывающую пошаговую инструкцию** для автоматической генерации КТП на основе данных из рабочих программ профессиональных модулей. ИИ должен следовать каждому шагу строго последовательно, не пропуская ни одного правила.

---

## 1. Общие принципы

### 1.1. Исходные данные

- **Рабочие программы (РП)** находятся в папке `RP/` (`.docx`/`.doc`/`.txt`).
- Каждая РП описывает один **профессиональный модуль (ПМ)**, который может содержать один или несколько **междисциплинарных курсов (МДК)**.
- Для каждого МДК создаётся отдельный КТП.

### 1.2. Выходные данные

- Для каждого МДК создаётся один файл КТП в формате **DOCX** (`.docx`).
- Файл сохраняется в папку `KTP/` с именем по шаблону:
  ```
  КТП МДК [код МДК] М[номер курса] курс.docx
  ```
  Примеры: `КТП МДК 05.02 М2 курс.docx`, `КТП МДК 03.01 М3 курс.docx`.
- Для генерации использовать канонический скрипт:
  ```bash
  python3 scripts/generate_ktp_docx.py --data data.json --output "KTP/КТП МДК 05.02 М2 курс.docx"
  ```
- После генерации **обязательно** запускать валидатор:
  ```bash
  python3 scripts/validate_ktp.py --doc "файл.docx" --data data.json
  ```
- Формат JSON-данных описан образцом `scripts/data_05_02_example.json`.

### 1.3. Эталон

- **Единственный эталон оформления** — `KTP/КТП МДК 05.02 М2 курс.docx`.
- При любых вопросах о формате (структура таблиц, шрифты, тексты шапок, объединения ячеек) — **сверяться с эталоном**, а не угадывать.
- `template_ktp.md` (пустая заготовка) и `sample_ktp_example.md` (правила заполнения) — справочные файлы для ИИ.

---

## 2. Порядок обработки одной РП

### Шаг 1. Определить перечень МДК в РП

1. Найти в РП раздел **«Контроль знаний по профессиональному модулю»** (обычно п. 1.3 паспорта модуля).
2. В таблице контроля знаний найти все строки с «МДК».
3. Для каждого МДК записать: **код и полное наименование**, **форму промежуточной аттестации** (дифференцированный зачёт, экзамен и т.д.).

### Шаг 2. Извлечь суммарные часы по каждому МДК

1. Найти раздел **«3.1. Тематический план профессионального модуля»**.
2. Для каждого МДК извлечь: общий объём, часы во взаимодействии с преподавателем, теоретическое обучение, лабораторные и практические занятия, курсовые, самостоятельную работу, учебную/производственную практику — **по семестрам** (для Таблицы 1).
3. Определить **курс и семестры** для группы (например, семестры 3–4 = 2 курс).

> **Важно:** самостоятельная работа **НЕ включается** в КТП — только аудиторные занятия.

### Шаг 3. Извлечь темы и часы из раздела «Содержание обучения»

1. Найти раздел **«3.2. Содержание обучения»** (или «2.2»).
2. Для каждой темы извлечь: номер и наименование, общие часы, часы теории, часы практики, содержание, практические занятия с номерами и названиями.

### Шаг 4. Определить вид каждого занятия

| Тип занятия | Признак в РП | Обозначение в КТП |
|---|---|---|
| Теоретическое (урок) | Часы в колонке «теоретическое обучение» | `Урок` |
| Практическое занятие | Пометка «Практическое занятие» | `Практ. занятие` |
| Лабораторная работа | Пометка «Лабораторная работа» | `Лаб. работа` |

### Шаг 5. Извлечь компетенции и литературу

1. Из паспорта ПМ: коды **ОК** (общие) и **ПК** (профессиональные) компетенций.
2. Из раздела «Учебно-методическое и информационное обеспечение»: литература и электронные источники.
3. Из раздела «Условия реализации»: материально-техническое обеспечение.

---

## 3. Правила формирования занятий

### 3.1. Длительность

- Каждое занятие (пара) — **ровно 2 академических часа**.
- Тема на 4 часа = 2 занятия; на 6 часов = 3 занятия и т.д.

### 3.2. Разбивка теоретических тем

- 2 часа — одно занятие с полным названием темы.
- Более 2 часов — разбивка на части: `... (часть 1)`, `... (часть 2)` и т.д.
- **Исключение:** если в РП тема уже разбита на подтемы — использовать названия подтем.

### 3.3. Практические занятия

- Берутся **напрямую из РП** с оригинальными названиями и номерами.
- Без нумерации в РП — нумеровать сквозно в рамках МДК.
- 4 часа = **два занятия** с одинаковым названием.

### 3.4. Лабораторные работы

- Оформляются аналогично практическим; вид занятия — `Лаб. работа`.

### 3.5. Промежуточная аттестация

- **Одна строка** в конце Таблицы 2 (последнее занятие, со сквозным номером).
- Название строки — форма аттестации (например, `Дифференцированный зачет`), **жирным**.
- Часы — `2`.
- Графы **ОК, ПК, Вид занятия, Обеспечение, Задания, Контроль — ПУСТЫЕ** (строго по эталону).

### 3.6. Самостоятельная работа

- Строки самостоятельной (внеаудиторной) работы из РП **полностью игнорируются**.

---

## 4. Титульный лист

### 4.1. Реквизиты (фиксированные, строго по эталону)

```
МИНИСТЕРСТВО ОБРАЗОВАНИЯ РОСТОВСКОЙ ОБЛАСТИ
```
⚠️ **БЕЗ** слов «общего и профессионального» — это частая ошибка старых документов.

```
ГОСУДАРСТВЕННОЕ БЮДЖЕТНОЕ ПРОФЕССИОНАЛЬНОЕ ОБРАЗОВАТЕЛЬНОЕ УЧРЕЖДЕНИЕ РОСТОВСКОЙ ОБЛАСТИ
«САЛЬСКИЙ ИНДУСТРИАЛЬНЫЙ ТЕХНИКУМ»
(ГБПОУ РО «СИТ»)
```

### 4.2. Утверждающие подписи

Безрамочная таблица, выровненная вправо (текст в 3-й колонке):
```
УТВЕРЖДАЮ
Зам. директора ГБПОУ РО «СИТ» ______________ Т.В. Якимова
«____» ______________ 20 ____ г.
```

### 4.3. Заполняемые поля (значения подчёркнуты)

- `на __3-4__ семестр(ы) _2026___ - __2027___ учебного года _____2______ курс`
- `учебной группы (учебных групп) ___________ М-21____________________`
- `Профессиональный модуль: ПМ.05 [полное наименование]`
- `Междисциплинарные курсы: МДК 05.02 [полное наименование]`
- `по профессии: [код и наименование из РП]`

### 4.4. Блок часов

```
Объем образовательной программы: ______112______________ (часов);
в том числе в форме практической подготовки ___60______________(часов);
Учебная нагрузка во взаимодействии с преподавателем________112_______________ (часов):
из нее:
теоретическое обучение __50___ (часов);    практические занятия __60__ (часов);
лабораторные занятия _______ (часов);      курсовая работа/проект _______ (часов);
самостоятельная работа _______ (часов);
промежуточная аттестация в форме __ Комплексный дифференцированный зачет   _2__(часа).
                                                                                  (указать форму)
```

> **Контроль:** теория + практика + часы аттестации = общий объём МДК.
> (В эталоне: 50 + 60 + 2 = 112.)

> **ИСТОЧНИК ЧАСОВ (решение владельца):** цифры берутся из строки
> **«МДК 05.02 …» тематического плана РП** (раздел 3 РП, таблица
> «3.2 Тематический план и содержание профессионального модуля»):
> - **«Объем образовательной программы» (112)** = кол. 3 этой строки;
> - **«в том числе в форме практической подготовки» (60)** = **кол. 4 этой строки** —
>   та же цифра, что и сумма колонки 4 Таблицы 2 КТП (60 = 60);
> - «практические занятия» (60) = той же кол. 4.
>
> **НЕ путать:** 96 = 60 (лаб./практ. МДК) + 36 (учебная практика УП.05) — это
> практическая подготовка Раздела 2 целиком по табл. 3.1 РП. Учебная практика
> планируется отдельным документом и в КТП по МДК не входит, поэтому на титуле
> КТП и в Таблице 2 — **60**.
>
> **Склонение слова «час»** в скобках титула подставляется по числу
> (`hour_word` в генераторе): 1 → час, 2–4 → часа, 5–20 и десятки → часов
> (например, 8 → часов, 2 → часа, 60 → часов, 112 → часов, 22 → часа).
> В JSON: `total_hours` = 112, `practice_hours` = 60.

### 4.5. Низ титульного листа

```
Составлен в соответствии с рабочей программой ПМ 05, утверждённой __________________
              (дата утверждения)
Рассмотрен на заседании цикловой комиссии       технических    дисциплин
Протокол  №_______от________________20_____ года
Председатель цикловой комиссии ___________________________/Ткаченко А.Н./

г. Сальск
2026 – 2027 уч. год
```

**Стиль:** все строки шапки и блока часов — стиль `Заголовок1`, Times New Roman 12pt; министерство/техникум и «КАЛЕНДАРНО-ТЕМАТИЧЕСКИЙ ПЛАН» — по центру и жирным; заполняемые поля — по левому краю.

---

## 5. Таблица 1. Распределение часов по профессиональному модулю

Расположение: новая страница, заголовки `Распределение часов по профессиональному модулю` (по центру, жирно) и `Таблица 1` (справа).

**Структура: 12 колонок, 12 строк.**
- Шапка 4 строки с объединениями:
  - R0: «Междисциплинарный курс (индекс МДК)» (vMerge R0–R3), «Курс», «Семестр», «Объём времени, отведённый на освоение междисциплинарного курса» (7 колонок), «Практика» (2 колонки);
  - R1: «Объем образовательной программы», «Учебная нагрузка во взаимодействии с преподавателем» (5 колонок), «Самостоятельная работа обучающегося, часов», «Учебная, часов», «Производственная, часов»;
  - R2: «Всего, часов», «в т.ч.» (4 колонки);
  - R3: «Теоретические занятия», «лабораторные работы, часов», «практические занятия, часов», «Курсовые работы (проекты), часов».
- R4 — номерная строка `1..12` (не жирная).
- Строки семестров (из таблицы 3.1 РП) — **жирные**; отсутствующие значения — `-`.
- 3 резервные пустые строки (итого в таблице всегда 12 строк при 2 семестрах).
- Строка «Практика» — все прочерки.
- Строка «Всего» — суммы; **теория = Σ теории по семестрам + часы аттестации** (в эталоне 50 + 2 = 52).

Под таблицей — подпись:
```
(МДК 05.02 Полное наименование МДК) – (Комплексный дифференцированный зачет).
```

---

## 6. Таблица 2. Содержание обучения по профессиональному модулю

Заголовки: `Содержание обучения по профессиональному модулю` (жирно) и `Таблица 2`. Новая страница.

**Структура: 11 колонок.** Шапка — 3 строки с объединениями (gridSpan/vMerge) + номерная строка `1..11`:

```
R0: [№ занятия] [Наименование разделов профессионального модуля, тем и занятий по МДК]
    [Обязательная учебная нагрузка → 3 колонки] [Коды формируемых компетенции → 2 колонки]
    [Материальное и информационное обеспечение занятий] [Задания для студентов]
    [Формы и методы контроля] [ФИО преподавателя]
R1: под «нагрузкой»: Кол-во часов | В форме практической подготовки | Вид занятия
    под «кодами»: объединение продолжается
R2: под «кодами»: ОК | ПК
R3: номера граф 1..11 (не жирные)
```

Тексты шапки — строго как в эталоне (см. `sample_ktp_example.md`).

### 6.1. Типы строк

1. **Строка МДК** (жирная): название, общие часы, часы практики, **ФИО преподавателя в графе 11**.
2. **Строка темы** (жирная): название, часы темы, часы практики.
3. **Строки занятий** (обычные): все графы заполнены.
4. **Строка аттестации**: номер, жирное название, 2 часа, остальные графы пустые.
5. **Строка «Итого:»** (жирная): сумма часов и суммы практики.

### 6.2. Правила заполнения граф

| Графа | Название | Правила |
|---|---|---|
| 1 | № занятия | Сквозная нумерация 1, 2, 3…; диф.зачёт тоже нумеруется. |
| 2 | Наименование | Полное название из РП; разбивка на части — «(часть N)». |
| 3 | Кол-во часов | Всегда `2`. |
| 4 | В форме практической подготовки | **Строго построчно из РП, табл. 3.2 (кол. 4)**: значение строки «В т.ч. практических занятий и лаб» темы → в заголовок «Тема 2.X»; значение кол. 4 занятия РП → в строку занятия (занятие РП на 4 ч, разбитое в КТП на «часть 1/часть 2», переносится по частям с сохранением суммы). Теория и аттестация — пусто (в РП у них кол. 4 пустая). Контроль: Σ по каждой теме = заголовку темы; Σ всех = практике МДК (для 05.02: 60). |
| 5 | Вид занятия | `Урок` / `Практ. занятие` / `Лаб. работа`. |
| 6 | ОК | Коды общих компетенций (одинаковы во всех занятиях). |
| 7 | ПК | Коды профессиональных компетенций. |
| 8 | Обеспечение | Теория: код одной книги (`ОИ2`). Практика: `№3, 2а`. |
| 9 | Задания | Теория: `Стр. X-Y` (реальные страницы из оглавления). Практика: `Метод. указания`. |
| 10 | Формы и методы контроля | Первое занятие темы: `Вх.контр`. Остальная теория: `Текущий контроль`. Практика: `Защита пр.раб.`. Аттестация: пусто. |
| 11 | ФИО преподавателя | Только в строке МДК. |

---

## 7. Дополнительные таблицы

### 7.1. Таблица 2а — Материально-техническое обеспечение

```
Материально-техническое обеспечение занятий
Таблица 2а
```
2 колонки; шапка (не жирная) + номерная строка `1 | 2` (**жирная**) + данные (№ по центру, наименование по левому краю). Данные — из раздела «Материально-техническое обеспечение» РП.

### 7.2. Таблица 2б — Основные источники (ОИ)

4 колонки: `№ п/п | Наименование | Автор | Издательство, год издания`. Только книги **не старше 5 лет** (для 2026 года — изданные в 2021+). Коды: `ОИ 1`, `ОИ 2`, …

### 7.3. Таблица 2в — Дополнительные источники (ДИ)

4 колонки, формат как 2б. Печатная литература, включая перенесённую из ОИ по возрасту.

### 7.4. Таблица 2г — Электронные источники (ЭИ)

**5 колонок** — дополнительная колонка `URL (ссылка)`:

```
| № п/п | Наименование | Автор | Издательство, год издания | URL (ссылка) |
| ЭИ 1 | Электрические системы и сети… | Климова Г.Н. | М.: Юрайт, 2023 | https://urait.ru/bcode/513864 |
| ЭИ 2 | Информационный портал для электриков | | | http://electromonter.info |
```

Для сайтов без автора/издательства — столбцы пустые, только URL.

> ⚠️ В эталоне таблица ЭИ по ошибке подписана «Электроные источники» и повторной меткой «Таблица 2в». При генерации эти опечатки **исправляются**: «Электронные источники (ЭИ):» и «Таблица 2г».

---

## 8. Правило свежести литературы (5-летний рубеж)

Текущий год: **2026**. Литература, изданная **до 2021 года**, переносится из ОИ в ДИ.

Алгоритм:
1. Из списка ОИ РП удалить книги с годом издания < 2021.
2. Перенести их в ДИ с новой нумерацией.
3. Перенумеровать: ОИ 1…, ДИ 1…, ЭИ 1… (сквозная, без пропусков).
4. Для поиска страниц и URL использовать веб-поиск (см. KTP_INSTRUCTIONS.md, раздел 9).

---

## 9. Оформление (сводка, строго по эталону)

- **Шрифт**: Times New Roman; титульник 12pt; таблицы 10pt; наименования литературы 9pt.
- **Заливка ячеек**: отсутствует (все белые) — выделение только жирным.
- **Выравнивание**: в таблицах по центру; графа 2 и наименования литературы — по левому краю; вертикально — по центру.
- **Разрывы страниц**: после титульника; перед Таблицей 1 (сразу после них идут её заголовки — на 2-й странице); перед Таблицей 2; перед Таблицей 2а; перед блоком «Информационное обеспечение обучения». Всего 4 разрыва.
- **Таблицы без колонки «Примечание»** — графы контроля называются «Формы и методы контроля».
- Контроль практики: `Защита пр.раб.` (НЕ «Защита пр.работы»).

---

## 10. Итоговая проверка (ОБЯЗАТЕЛЬНЫЙ ЭТАП)

Запускать валидатор `scripts/validate_ktp.py` (17 проверок) и убедиться вручную:

### 10.1. Суммы часов
1. Σ часов всех занятий (включая аттестацию) = общему объёму МДК.
2. Σ графы 4 = плановой практической подготовке.
3. Теория + практика + аттестация = общий объём.

### 10.2. Структура таблиц
1. 7 таблиц: УТВЕРЖДАЮ, Т1 (12 колонок), Т2 (11 колонок), 2а (2), 2б (4), 2в (4), 2г (5).
2. Сумма gridSpan каждой строки Т1 = 12, Т2 = 11.
3. Т1 — 12 строк; шапки и тексты совпадают с эталоном.

### 10.3. Полнота
1. Все темы и практические занятия из РП присутствуют.
2. Самостоятельная работа отсутствует.
3. Нумерация сквозная, без пропусков.

### 10.4. Литература
1. В ОИ нет книг старше 5 лет; ДИ содержит перенесённые; ЭИ — с URL.
2. Нумерация ОИ/ДИ/ЭИ сквозная.

### 10.5. OOXML
Файл — валидный ZIP (`testzip()` без ошибок).

---

## 11. Обработка нескольких РП

1. Получить список файлов в `RP/`; для каждого выполнить шаги 2–10.
2. Если один и тот же МДК упоминается в разных РП — использовать ту, где он основной.
3. Готовый КТП сохранять в `KTP/` с именем `КТП МДК [код] М[курс] курс.docx`.

---

## 12. Дополнительные указания

- При неоднозначностях — решение **в пользу эталона и данного файла**; если данных нет вовсе — задать вопрос пользователю, а не угадывать.
- Если в РП нет данных для графы — оставить пустое место (`_____`) для ручного заполнения.
- Для всех числовых значений использовать **целые числа**.
- Фиксированные данные: ГБПОУ РО «СИТ»; зам. директора — Т.В. Якимова; председатель ЦК — Ткаченко А.Н.; город — г. Сальск.
- Канонические инструменты: `scripts/generate_ktp_docx.py` (генерация), `scripts/validate_ktp.py` (проверка), `scripts/data_05_02_example.json` (образец данных).
